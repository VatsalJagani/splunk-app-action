# ---- Project Info and Dependencies ----

[project.urls]
Repository = "https://github.com/VatsalJagani/splunk-app-action"
Homepage = "https://github.com/VatsalJagani/splunk-app-action"
Documentation = "https://splunk-app-action.readthedocs.io/"
Changelog = "https://github.com/VatsalJagani/splunk-app-action/blob/main/CHANGELOG.md"

[project]
name = "splunk-app-action"
description = "A custom python-based GitHub Action."
authors = [
    { name="Vatsal Jagani", email="vatsaljagani85@gmail.com" },
]
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.12,<3.14"
dynamic = ["version"]


# ---- Not for PyPI publishing ----
# This repository is NOT intended to be published as a Python package on PyPI.
# If you attempt to build and upload to PyPI, the upload should be blocked.


# ---- Main dependencies ----

dependencies = [
    "github-action-toolkit>=0.8.0",
    "pip>=25.3",
    "requests>=2.32.5",
    "splunk-add-on-ucc-framework>=6.3.0",
    "splunk-appinspect>=2.0.0",
    "python-magic==0.4.24",
]


# ---- Dev dependencies ----

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-sugar>=1.0.0",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.4",
    "codespell>=2.4.1",
    "rich>=14.0.0",
    "basedpyright>=1.33.0",
    "funlog>=0.2.1",
    "sphinx>=9.1.0",
    "sphinx-autobuild>=2025.8.25",
    "sphinx-autodoc-typehints>=3.4.0",
    "sphinx-copybutton>=0.5.2",
    "sphinxcontrib-mermaid>=1.0.0",
    "furo>=2025.9.25",
    "myst-parser>=4.0.1",
]

[project.scripts]
# Add script entry points here:
splunk-app-action = "src:main"


# NOTE - No Build System settings here because this is not a python package to be published on PyPI.


# ---- Settings ----

[tool.ruff]
# Set as desired, typically 88 (black standard) or 100 (wide).
line-length = 100
exclude = [
    "tests/test_app_repos",
    "tests/integration_test_apps",
    "tests/dummy_utility_files",
    "tests/splunk_config_files"
]

[tool.ruff.lint]
select = [
    # See: https://docs.astral.sh/ruff/rules/
    # Basic list from: https://docs.astral.sh/ruff/linter/#rule-selection
    "E", # https://docs.astral.sh/ruff/rules/#error-e
    "F", # https://docs.astral.sh/ruff/rules/#pyflakes-f
    "UP", # https://docs.astral.sh/ruff/rules/#pyupgrade-up
    "B", # https://docs.astral.sh/ruff/rules/#flake8-bugbear-b
    "I", # https://docs.astral.sh/ruff/rules/#isort-i
    # Other possibilities:
    # "D" # https://docs.astral.sh/ruff/rules/#pydocstyle-d
    # "Q" # https://docs.astral.sh/ruff/rules/#flake8-quotes-q
    # "COM" # https://docs.astral.sh/ruff/rules/#flake8-commas-com
    # "SIM", # https://docs.astral.sh/ruff/rules/#flake8-simplify-sim

]
ignore = [
    # Disable some rules that are overly pedantic. Add/remove as desired:
    "E501", # https://docs.astral.sh/ruff/rules/line-too-long/
    "E402", # https://docs.astral.sh/ruff/rules/module-import-not-at-top-of-file/
    "E731", # https://docs.astral.sh/ruff/rules/lambda-assignment/
    # We use both ruff formatter and linter so some rules should always be disabled.
    # See: https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    "W191", # https://docs.astral.sh/ruff/rules/tab-indentation/
    "E111", # https://docs.astral.sh/ruff/rules/indentation-with-invalid-multiple/
    "E114", # https://docs.astral.sh/ruff/rules/indentation-with-invalid-multiple-comment/
    "E117", # https://docs.astral.sh/ruff/rules/over-indented/
    "D206", # https://docs.astral.sh/ruff/rules/docstring-tab-indentation/
    "D300", # https://docs.astral.sh/ruff/rules/triple-single-quotes/
    "Q000", # https://docs.astral.sh/ruff/rules/bad-quotes-inline-string/
    "Q001", # https://docs.astral.sh/ruff/rules/bad-quotes-multiline-string/
    "Q002", # https://docs.astral.sh/ruff/rules/bad-quotes-docstring/
    "Q003", # https://docs.astral.sh/ruff/rules/avoidable-escaped-quote/
    "COM812", # https://docs.astral.sh/ruff/rules/missing-trailing-comma/
    "COM819", # https://docs.astral.sh/ruff/rules/prohibited-trailing-comma/
    "ISC002", # https://docs.astral.sh/ruff/rules/multi-line-implicit-string-concatenation/
]

[tool.basedpyright]
# BasedPyright currently seems like the best type checker option, much faster
# than mypy and with a good extension for VSCode/Cursor.
# https://marketplace.visualstudio.com/items?itemName=detachhead.basedpyright
# https://docs.basedpyright.com/latest/configuration/config-files/#sample-pyprojecttoml-file
include = ["src", "tests", "devtools"]
# TODO - Ideally tests folder also should be checked for type checking, but filtering for this project
exclude = [
    "tests/test_app_repos",
    "tests/integration_test_apps",
    "tests/dummy_utility_files",
    "tests/splunk_config_files",
    "src/utilities/logger/logger_manager.py"
]
# By default BasedPyright is very strict, so you almost certainly want to disable
# some of the rules.
# First, these turn off warnings about (yes) how you ignore warnings:
reportIgnoreCommentWithoutRule = false
reportUnnecessaryTypeIgnoreComment = false
# A few typically noisy warnings are next.
# How many you enable is up to you. The first few are off by default, but you can
# comment/uncomment these as desired:
reportMissingTypeStubs = false
reportUnusedCallResult = false
reportAny = false
reportExplicitAny = false
reportImplicitStringConcatenation = false
reportUnreachable = false
reportImplicitRelativeImport = false
# reportPrivateImportUsage = false
# reportPrivateLocalImportUsage = false
# reportMissingImports = false
# reportUnnecessaryIsInstance = false
# reportUnknownVariableType = false
# reportUnknownArgumentType = false

[tool.codespell]
# Add here as needed:
# ignore-words-list = "foo,bar"
# skip = "foo.py,bar.py"

[tool.pytest.ini_options]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
testpaths = [
    "tests"
]
norecursedirs = [
    "tests/integration_test_apps",
    "tests/test_app_repos",
    "tests/dummy_utility_files",
    "tests/splunk_config_files"
]
filterwarnings = []
# Disable pytest-sugar plugin to avoid FileNotFoundError issues
addopts = ["-p", "no:sugar"]
