# Valid Test App (User Commands)

This is a valid Splunk app used for testing the splunk-app-action GitHub Action with user-defined commands.

## Purpose
This app should pass all app-inspect checks and be used to test:
- User-defined command execution
- Custom build steps via SPLUNK_APP_ACTION_* environment variables
- Command execution in app directory context
